name: lunar_lander
target: srccode.system.LunarLander
reference: = [0., 1., 0., 0., 0., 0.]
observer: = srccode.observer.ObserverReference(${.reference})
dim_observation: = ${.target}().dim_observation
dim_action: = ${.target}().dim_inputs

time_final: 10.
sampling_time: 0.01
action_bounds: = [[-100., 100.],[-50, 50]]
